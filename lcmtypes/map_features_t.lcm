struct map_features_t
{
    int64_t utime;
    
    
    int32_t nlineSegs; //number of line segments
    //each line segment has two end points hence 4 coordinates
    //the convention to use is x1,y1,x2,y2
    double lineSegs[nlineSegs][4];

    int32_t ntriangles;//number of triangle features
    //center of triangle
    double triangles[ntriangles][2];

    bot_status_t bot;
}
